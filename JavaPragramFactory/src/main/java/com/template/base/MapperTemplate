<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="#{namespace}">

	<sql id="#{sqlColumId}">
	#{columList}
	</sql>
	<sql id="#{sqlWhereId}">
		<trim>
			<if test="#{columField}">
			#{columField}
			</if>
		</trim>
	</sql>
	
	<sql id="#{selectFromId}">
	#{fromTable}
    </sql>
    
    #{selectLabelContent}
    
    #{deleteLabelContent}
    
    #{updateLableContent}
	
    #{insertLableContent}
	
    #{resultMapLableContent}

</mapper> 